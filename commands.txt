./build.sh
docker-compose build --no-cache backend
docker-compose build --no-cache web-cabinet-bind
docker-compose build --no-cache web-test-tool
docker images | grep device-relatedtools
 docker-compose logs backend
curl -u admin:admin123 http://localhost:15672/api/overview
docker exec powerbank-rabbitmq rabbitmq-plugins enable rabbitmq_delayed_message_exchange
docker exec powerbank-rabbitmq rabbitmq-plugins list
docker exec powerbank-rabbitmq rabbitmq-plugins list | grep delayed

docker compose up -d backend web-cabinet-bind web-test-tool



# Enable the nginx site
sudo ln -sf /etc/nginx/sites-available/powerbank-api.chargeghar.com /etc/nginx/sites-enabled/

# Test nginx configuration
sudo nginx -t

# Reload nginx
sudo systemctl reload nginx

# Get SSL certificate
sudo certbot --nginx -d powerbank-api.chargeghar.com --non-interactive --agree-tos --email admin@chargeghar.com


# Use development compose file
docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d backend

# Now code changes auto-reload in ~10-30 seconds!


docker compose up -d --no-deps --build backend

